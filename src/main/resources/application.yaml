spring:
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 7200 # 5 min
    refreshTokenExpiration: 604800 # 7 Days
  application:
    name: store
  datasource:
    url: jdbc:postgresql://localhost:5432/store_api
    username: postgres
    password: admin
  jpa:
    show-sql: true
stripe:
  secret: ${STRIPE_SECRET}
websiteUrl: http://localhost:8080